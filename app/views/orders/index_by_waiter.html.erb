<%= render 'layouts/header', title: 'Orders by waiter' %>

<div class="container-fluid">
  <div class="row">
    <% # Note: The sidebar takes up 3 cols (out of 12) %>
    <%= render 'layouts/sidebar' %>

    <div class="col-lg-9">

      <%= bootstrap_form_tag(url: index_by_waiter_id_path, method: :get, layout: :horizontal, label_col: "col-lg-3", control_col: "col-lg-3") do |f| %>

        <%= f.select :waiter_id, Waiter.all.map { |e| [e.name, e.id] }, { prompt: "Select waiter", label: "Waiter to show orders of" } %>

        <%= f.form_group do %>
          <%= f.submit "Show orders" %>
        <% end %>
      <% end %>

      <table class="table table-bordered table-striped">
          <thead>
              <tr>
                  <th>Table id</th>
                  <th>Food</th>
                  <th>Waiter</th>
                  <th>Time</th>
                  <th>Cost</th>
                  <th>Delivered</th>
              </tr>
          </thead>
          <tbody>
              <%= render @orders %>
          </tbody>
      </table>
    </div>
  </div>
</div>
