<%= render 'layouts/header', title: 'Delete Table' %>

<div class="container-fluid">
	<div class="row">
		<% # Note: The sidebar takes up 3 cols (out of 12) %>
		<%= render 'layouts/sidebar' %>

		<div class="col-lg-6">
			<h5><%= link_to "Confirm Delete", table_path(id: @table.id), method: :delete %></h5>
		</div>
	</div>
</div>