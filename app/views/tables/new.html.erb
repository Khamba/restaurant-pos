<%= render 'layouts/header', title: "New Table" %>

<div class="container-fluid">
	<div class="row">
		<% # Note: The sidebar takes up 3 cols (out of 12) %>
		<%= render 'layouts/sidebar' %>

		<div class="col-lg-4">
			<h5> This is table number <%= Table.all.count + 1 %> you are adding.</h5>
			<br>
			<%= link_to "Continue",tables_path, method: :post %> 
		</div>

	</div>
</div>